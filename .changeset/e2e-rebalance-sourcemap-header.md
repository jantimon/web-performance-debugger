---
---

Test/infra only: move the browser-free CLI wiring tests (flag-rejection guards,
removed-verb spawn stubs, `--target node` lane) into a fast unit file, and add an
e2e for the remote SourceMap-response-header path (a minified bundle split by package).
