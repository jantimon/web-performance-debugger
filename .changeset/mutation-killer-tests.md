---
---

Add unit tests pinning the step-view count mapping, the scan.ts string-escape skip, the sampled-blame line/interval boundaries, the span-merge measure-only guard, and the compat localhost ephemeral-port fold. Tests only; no runtime change.
