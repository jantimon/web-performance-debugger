---
---

Docs-only: restructure `CLAUDE.md`. Collapse the per-feature test inventory to the three lanes and
their self-skip contract, compress the CPU-profiling section to its measured prohibitions plus the
non-obvious constraints (linking `docs/dev/` for the mechanism), and reconcile the file/module map
with the current tree (`record/options.ts`, `cli-validation.ts`, `commands/query-view.ts`,
`trace/scope.ts`, the current `docs/dev/` inventory). No behavior change.
