---
---

Tooling only, no package change: the changeset changelog generator switches to
`@changesets/changelog-github` with author credits disabled, so each `CHANGELOG.md` entry links its
pull request.
