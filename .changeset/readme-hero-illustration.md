---
"@jantimon/web-performance-debugger": patch
---

Add a hand-drawn intro illustration to the README: a browser, a `wpd record --breakdown` terminal,
and the reconciling `Σ slices + idle = wall` bar, so the core idea lands at a glance before the demo.
