---
---

Docs-only: new `docs/dev/measurement-ecosystem.md` (what the major tools do about headless,
throttling, frame cadence, and variance, as context for wpd's methodology), a surfaces-evaluated
register in `orchestrator-boundary.md`, the boot-LCP entry-delivery race in `navigation-and-lcp.md`,
the sandbox/trace CI deadlock in `trace-buffer.md`, and a ledger row. No behavior change.
