---
---

Add a `knip` dead-export/file/dependency gate to the browser-free `ci` job (`npm run knip`,
config in `knip.json`). Dev tooling only, so this changeset is empty: no runtime, API, or CLI change.
