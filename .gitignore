node_modules/
dist/
recordings/
.wpd-profile/
*.png
# The routing-section terminal shots are committed (they render on GitHub and npm rewrites the
# relative paths via the repository field); the render's throwaway GIFs are not.
!examples/demo-gif/shots/
!examples/demo-gif/shots/*.png
examples/demo-gif/*.gif
*.tsbuildinfo
*.tgz
.DS_Store

# CLAUDE.md is checked in on purpose: it is this repo's architecture map, and it points at
# docs/dev/ for the measured facts behind the non-obvious choices. Many contributors ignore
# CLAUDE.md globally (core.excludesFile), so un-ignore it here -- repo rules win over global.
!CLAUDE.md
