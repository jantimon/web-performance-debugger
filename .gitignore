node_modules/
dist/
recordings/
.wpd-profile/
*.png
# The VHS demo render writes throwaway GIFs next to demo.tape; demo.gif is hosted, never committed.
examples/demo-gif/*.gif
*.tsbuildinfo
*.tgz
.DS_Store

# CLAUDE.md is checked in on purpose: it is this repo's architecture map, and it points at
# docs/dev/ for the measured facts behind the non-obvious choices. Many contributors ignore
# CLAUDE.md globally (core.excludesFile), so un-ignore it here -- repo rules win over global.
!CLAUDE.md

# one-off probe scratch dirs (never ship / commit)
scratch*/

# host-cpu-matrix probe output (scripts/host-cpu-probe.mjs writes it to cwd; ephemeral, uploaded as a
# CI artifact, never committed)
